Terraform Mumbai region


terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# Provider configured for AWS Mumbai
provider "aws" {
  region = "ap-south-1"
}

# Example infrastructure resource in Mumbai
resource "aws_s3_bucket" "example" {
  bucket = "macscloud-mumbai-demo-bucket"
}

